{
  "name": "defi-circuit-breakers",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format": "forge fmt",
    "solhint": "solhint --config ./.solhint.json 'src/**/*.sol' --fix",
    "lint": "forge fmt && npm run solhint"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "prettier-plugin-solidity": "^1.0.0-beta.19",
    "solhint": "^3.3.6"
  }
}
